return {
    -- https://github.com/szw/vim-maximizer
    'szw/vim-maximizer',
    event = 'VeryLazy',

    config = function()
        -- No extra options required, vim-maximizer works out of the box
        -- You can optionally configure keymaps or behavior here if needed
    end,
}
