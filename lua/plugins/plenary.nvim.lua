return {
    "nvim-lua/plenary.nvim",
    config = function()
        -- You can put any custom configuration for plenary here if needed
        -- plenary is often used as a utility library by other plugins, so it may not need much configuration by itself
    end
}
