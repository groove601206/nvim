# neovim

 it primarily for python programming, but you can use it for lua as well, don't forget to check mason if all installed such as pyright debugpy, black, isort, mypy etc
for lua stylua lua-language-server, and this is config for macos , if you need agnostic init.lua don't hesitate to ask me i'll help as much as i can. good luck.

```bash
mv ~/.config/nvim ~/.config/nvim.bak
mv ~/.local/share/nvim ~/.local/share/nvim.bak
mv ~/.local/state/nvim ~/.local/state/nvim.bak
mv ~/.cache/nvim ~/.cache/nvim.bak

git clone https://github.com/groove601206/nvim.git ~/.config/nvim
